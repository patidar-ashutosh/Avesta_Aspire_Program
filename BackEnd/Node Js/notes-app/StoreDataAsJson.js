const fs = require("fs");

/*
// create a object
const book = {
    title : "Ego is the Enemy",
    author : "Ryan Holiday"
}

// conver into json formate
const bookJSON = JSON.stringify(book);
// store data in new file
fs.writeFileSync("1stJson.json", bookJSON);
*/

/*
// read the the data from 1stJson.json
// readFileSync is return the Buffer
const dataBuffer = fs.readFileSync("1stJson.json");
// we need to convert it to in string
const dataJSON = dataBuffer.toString();
// convet json to object
const data = JSON.parse(dataJSON);
console.log(data.title);
*/